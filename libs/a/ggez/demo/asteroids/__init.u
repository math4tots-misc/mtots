import a.ggez
import .screen.play
import .model::Model

def main() {
    app = ggez::App(name='asteroids')
    model = Model()
    play::update(app, model)
    app.main()
}


if __name == __main() {
    main()
}
