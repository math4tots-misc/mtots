# The menu screen
import ..play
import ...model::Model
import ...model::Board


def update(app) {
    [R, C] = [20, 10]
    model = Model(Board(R, C))

    # For now, just jump straight to the play screen
    play::update(app, model)
}
